/* eslint-disable react/prop-types */
const PostBadge = ({ id }) => (
  <span className="text-xs px-2 py-1 bg-gray-100 dark:bg-gray-300 rounded-full text-gray-900 dark:text-gray-800">
    Post #{id}
  </span>
);

export default PostBadge;
